# Task: <一句話任務名>

## 目標
<要做什麼，具體到可驗收>

## 動機
<為什麼做；邊界情況依此自行取捨，不要回來問>

## 執行依據
先讀 repo 根的 `AGENTS.md`，照它指到的 skill/runbook 執行（若有 `.claude/skills/<name>/SKILL.md`，直接讀檔照步驟做）。

## 範圍宣告（硬邊界）
<!-- oc-scope: <允許路徑前綴，空格分隔> -->
（上一行供 `oc-score.sh` 機械核對，與下方人讀宣告一致；空格分隔的允許路徑前綴。
**不可省略**：缺這行或留空 → 範圍檢查無從執行，L1 判 `INCONCLUSIVE`，不是 PASS。）
- 只可新增/修改：<目錄清單>
- 禁止觸碰：<禁區清單，例如 scripts/、本任務檔以外的制度檔>
- 禁止 `git commit`（評分後由指揮官提交）

## 驗收條件（交付前逐條自查）
1. <條件…>
n. 執行 `<gate 指令>` 直到 **exit 0**，把最終輸出原文貼進回報。

## 預算意識
你的執行有硬性時間預算，超時會被強制終止、成果作廢。完成主體後**立即**收尾（收尾同步屬「完成」的一部分，優先於任何潤飾）；若進度緊迫，停止新增內容、轉入收尾，回報中如實列未盡項——誠實的部分完成勝過被斬斷的接近完成。禁止為省時跳過 `<gate 指令>`。

## 欄杆
- 同一問題修 2 次仍失敗 → 停下，把失敗軌跡寫進回報，不要硬撐。
- 不確定＝尚未完成；禁止因時間/預算把未完成標記為完成。
- 原文逐字引用：每處 ≤25 詞、至多兩個短句的 punchline；禁止逐字轉貼任何完整段落（其餘一律摘要重寫）；引註標示須如實（句數/詞數不得虛報）。

## 回報格式
照本 repo 的 `REPORT-PROTOCOL.md` 的 ①–⑥ 段落結構（**⑥ 指示摩擦必填**，無則明寫「⑥ 無」）。
**你的最終訊息就是全部回報，沒有人會追問。**
